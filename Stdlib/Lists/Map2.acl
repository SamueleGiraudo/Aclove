let lists = put Lists in
let map = put Map in
let combine = put Combine in
let compute = put ../Products/Compute in

'map_2 [
    @ %f %lst1 %lst2 -> map (compute %f) (combine %lst1 %lst2)
]
: (+%e1 ^ +%e2 ^ +%e3) ^ +lists +%e1 ^ +lists +%e2 ^ +lists +%e3

