let lists = put Lists in
let empty = put Empty in
let cell = put Cell in

'fold_left [
    @ %f %s empty -> %s;
    @ %f %s (cell %x %lst) -> @ %f (%f %s %x) %lst
]
: (+%e1 ^ +%e2 ^ +%e1) ^ +%e1 ^ +lists +%e2 ^ +%e1

