let lists = put Lists in
let empty = put Empty in
let cell = put Cell in

'fold_right [
    @ %f empty %s -> %s;
    @ %f (cell %x %lst) %s -> %f %x (@ %f %lst %s)
]
: (+%e1 ^ +%e2 ^ +%e2) ^ +lists +%e1 ^ +%e2 ^ +%e2

