let lists = put Lists in
let empty = put Empty in
let cell = put Cell in
let append = put Append in

'flatten [
    @ empty -> empty;
    @ (cell %x %lst) -> append %x (@ %lst)
]
: +lists (+lists +%e) ^ +lists +%e

