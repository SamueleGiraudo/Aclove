let lists = put Lists in
let empty = put Empty in
let cell = put Cell in
let push_end = put PushEnd in

'reverse [
    @ empty -> empty;
    @ (cell %x %lst) -> push_end %x (@ %lst)
]
: +lists +%e ^ +lists +%e

